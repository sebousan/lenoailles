{{- $fields := slice 
  "heading" 
  "rows" 
  "grid" 
  "background" -}}
{
  label: Table,
  name: 'table',
  widget: 'object',
  required: false,
  i18n: true,
  collapsed: false,
  summary: '{{`{{heading.title}}`}}',
  {{ partial "admin/fields/_fields.yml" $fields }}
}
